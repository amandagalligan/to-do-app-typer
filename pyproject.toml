[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "rptodo"
version = "1.0.0"

[project.scripts]
rptodo = "rptodo.__main__:main"
